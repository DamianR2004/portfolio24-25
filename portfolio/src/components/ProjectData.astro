---
export interface Props {
    key: string;
    title: string;
    dates: string;
    description: string;
    image: string; 
}

const { title, dates, description, image } = Astro.props;

---

    {title}
    {dates}
    {description}
    {image}